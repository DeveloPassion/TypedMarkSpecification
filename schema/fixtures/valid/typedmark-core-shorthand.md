---
specification_version: 0.0.1
name: core-shorthand
description: Minimal Core Profile collection using defaulted boilerplate.
---

Valid `typedmark.md` shorthand. The omitted `metadata_directory`, `exclude_paths`,
and `validation_defaults` keys expand to their specification defaults.
