---
specification_version: 0.0.1
history:
  - version: 0.1.0
    changes:
      - op: add_field
        note_type: topic
        property_set: base
        field: title
---

INVALID SHAPE: a field operation must declare exactly one of note_type or property_set, not both.
This body is non-normative documentation; tools ignore it for structural reasoning.
