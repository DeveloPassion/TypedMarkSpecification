# INVALID SHAPE: a property set must not define the core-defined id field.
specification_version: 0.0.1
property_set: identified
description: Property set that wrongly declares id.
frontmatter:
  id:
    type: text
    format: slug
